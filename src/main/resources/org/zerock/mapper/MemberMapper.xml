<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.zerock.mapper.MemberMapper">
   <select id="getList" resultType="org.zerock.domain.MemberVO">
         select member_id, jumin, name, address, phone, email, reg_date from Member order by member_id desc
   </select>
   
   
	
	<select id="read" parameterType="string" resultType="org.zerock.domain.MemberVO">
	    SELECT * FROM member WHERE member_id = #{member_id}
	</select>
	
	<insert id="insert">
	  <selectKey keyProperty="member_id" order="BEFORE" resultType="int">
	    select seq_member_id.nextval from dual
	  </selectKey>
	  INSERT INTO member (member_id, jumin, name, address, phone, email, reg_date)
	  VALUES (seq_member_id.nextval, #{jumin}, #{name}, #{address}, #{phone}, #{email}, SYSDATE)
	</insert>

	

	<delete id="delete" parameterType="string">
   		 DELETE FROM member WHERE member_id = #{member_id}
	</delete>
	
	<update id="update" parameterType="org.zerock.domain.MemberVO">
	    UPDATE member
	    SET name = #{name},
	        address = #{address},
	        phone = #{phone},
	        email = #{email}
	    WHERE member_id = #{member_id}
	</update>
</mapper>