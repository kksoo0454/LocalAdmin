<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.zerock.mapper.NoticeMapper">
	<!-- 공지 리스트 -->
	<select id="getNoticeList" resultType="org.zerock.domain.NoticeVO">
		SELECT 
		    notice_id,
		    admin_name,
		    title,
		    content,
		    important,
		    created_at
		FROM 
		    notice
		ORDER BY 
		    notice_id ASC
	</select>
	<!-- 공지 등록 -->
	<insert id="insertNotice">
		INSERT INTO notice (
		    notice_id,
		    admin_name,
		    title,
		    content,
		    important,
		    created_at
		) VALUES (
		    notice_seq.NEXTVAL,
		    #{admin_name},
		    #{title},
		    #{content},
		    #{important},
		    SYSDATE
		)
	</insert>
	<!-- 공지 수정 -->
	<update id="updateNotice">
		UPDATE notice
		SET
		    title = #{title},
		    content = #{content},
		    important = #{important}
		WHERE
		    notice_id = #{notice_id}
	</update>
	<!-- 공지 읽기 -->
	<select id="readNotice" resultType="org.zerock.domain.NoticeVO">
		SELECT 
		    notice_id,
		    admin_name,
		    title,
		    content,
		    important,
		    created_at
		FROM 
		    notice
		WHERE 
		    notice_id = #{notice_id}
	</select>
	<!-- 공지 삭제 -->
	<delete id="deleteNotice" parameterType="int">
	  DELETE FROM notice
	  WHERE notice_id = #{notice_id}
	</delete>
</mapper>